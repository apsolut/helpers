#### Primary thigs to work with DDEV (first time install etc.)
    
    // first install had to disable HOST
    unset DOCKER_HOST


    // check do you have internet in docker: ping google.com
    // $ cd etc/ 
    sudo nano resolv.conf   // nameserver 1.1.1.1
